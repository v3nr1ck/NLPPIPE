# Core pipeline
pydantic>=2.0

# Dashboard
streamlit>=1.28

# Inference (optional — only needed for Ollama mode)
# No extra deps needed for urllib-based Ollama calls

# Fine-tuning (optional — only needed for train.py)
# unsloth[colab-new]
# trl<0.9.0
# peft
# accelerate
# bitsandbytes
# datasets

# Production inference with constrained generation (future)
# vLLM
# outlines
# outlines[vllm]
