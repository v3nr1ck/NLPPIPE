# Core pipeline
pydantic>=2.0

# RAG engine
sentence-transformers>=3.0

# Constrained generation (vLLM + Outlines — GPU required)
# vllm>=0.6.0
# outlines>=0.1.0

# Legacy dashboard (kept for developer use)
streamlit>=1.28

# Web app (FastAPI + HTMX)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Vendor profiles (optional)
# No extra deps — uses stdlib json
